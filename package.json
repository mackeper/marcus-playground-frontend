{
  "scripts": {
    "start": "live-server --port=3000 --entry-file=index.html --host=localhost",
    "build": "@powershell -NoProfile -ExecutionPolicy Unrestricted -Command ./build.ps1"
  },
  "devDependencies": {
    "live-server": "^1.1.0",
    "scss": "^0.2.4",
    "uglify-js": "^3.17.4"
  }
}
